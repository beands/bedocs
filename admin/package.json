{
  "name": "bedocs-admin",
  "version": "1.1.0",
  "description": "BeDocs Admin Panel — multi-project documentation management with fault-tolerant crea-ai AI generation (resumable background jobs, streaming, RAG)",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test test/*.test.js",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@fontsource-variable/inter": "5.3.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "lucide": "1.44.0",
    "multer": "^2.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
